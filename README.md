# roncato-app-shell

Static front-end shell. Deployment artifact only — built elsewhere, published here.

No credentials, no employee data, no business logic. Everything that decides
anything runs server-side and is unreachable from this repository.

`build.json` records the version, the source commit and the content hash of
`index.html`.
